<% content_for(:title, '掲示板一覧') %>
<div class='container pt-3'>
  <div class='row'>
    <div class='col-lg-10 offset-lg-1'>
      <!-- 検索フォーム -->
      <%= search_form_for @search, url: boards_path do |f| %>
        <%= render 'search_form', url: boards_path, f: f %>
      <% end %>
    </div>
  </div>
  <!-- 掲示板一覧 -->
  <div class='row'>
    <div class='col-12'>
      <div class='row'>
        <% if @boards.empty? %>
          <%= '掲示板はありません' %>
        <% else %>
          <%= render @boards %>
        <% end %>
      </div>
      <%= paginate @boards %>
    </div>
  </div>
</div>
