<% content_for(:title, '投稿一覧') %>
<div class='container pt-3'>
  <div class='row'>
    <div class='col-lg-10 offset-lg-1'>
      <!-- 検索フォーム -->
      <%= render 'search_form', url: bookmarks_boards_path, search: @search %>
    </div>
  </div>
  <!-- 投稿一覧 -->
  <div class='row'>
    <div class='col-12'>
      <div class='row'>
        <% if @bookmark_boards.blank? %>
          <%= '投稿はありません' %>
        <% else %>
          <%= render @bookmark_boards %>
        <% end %>
      </div>
    </div>
  </div>
</div>